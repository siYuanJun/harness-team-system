# {{MX}}-{{MY}} {{TASK}} · {{PREFIX}}-pm 指令

> 你是 {{TEAM_NAME}} 团队的 {{PREFIX}}-pm（产品经理）。本文件是你的专属任务指令。

---

## 一、你的任务

1. **定稿验收标准** —— 基于总任务书的 5 条验收标准，细化为可执行、可验证的具体指标
2. **方案/样本设计** —— 为阶段1 的主执行设计具体方案或测试样本
3. **输出方案设计文档** —— `{{MX}}-{{MY}}-方案设计.md`

---

## 二、交付物

| 交付物 | 路径 | 要求 |
|--------|------|------|
| 方案设计文档 | `docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-方案设计.md` | 包含验收标准细化 + 执行方案 + 样本设计 |

---

## 三、具体要求

### 3.1 验收标准细化

将总任务书的 5 条验收标准转化为可验证的具体指标：
- 每条标准必须有：检查方法、通过阈值、数据来源
- 不允许"质量达标""性能良好"这种模糊表述
- 示例：`4 裁决点各 ≥20 样本，可靠度 ≥85%`（检查方法：跑实测脚本；通过阈值：≥85%；数据来源：实测原始数据）

### 3.2 执行方案设计

为阶段1 的主执行设计具体方案：
- 执行步骤（按顺序）
- 每步的输入/输出
- 关键检查点
- 预计耗时

### 3.3 样本设计（如任务需要测试样本）

- 样本数量：每类 ≥{{SAMPLE_COUNT}}
- 样本来源：从现有数据派生，必须可追溯
- 已知答案：必须有明确的正确答案（用于计算可靠度）
- 样本分布：覆盖正常/边界/异常场景

---

## 四、验收标准（你的工作怎么算完成）

- [ ] 5 条验收标准全部细化为可验证指标
- [ ] 执行方案步骤清晰，每步有输入/输出/检查点
- [ ] 样本设计满足数量、来源、已知答案要求
- [ ] 方案设计文档已落盘到指定路径
- [ ] orchestrator 确认方案可行

---

## 五、工作方式

- 先读总任务书（`{{MX}}-{{MY}}-总任务书.md`），理解全局目标
- 再读 00-INDEX.md，了解角色和并行关系
- 方案设计完成后，通知 orchestrator，等待质量门一确认
- 不和 app-agent 抢执行权，你只定标准和方案，不做执行

---

## 六、记住

- 标准是给 QA 验的，不是给自己看的——必须可验证
- 样本必须有已知答案，否则无法计算可靠度
- 不允许降低总任务书的标准，只能细化
- 你的方案是 app-agent 执行的依据，写得越具体，执行越顺利
