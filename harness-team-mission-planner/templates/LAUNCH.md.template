# {{MX}}-{{MY}} {{TASK}} · Claude Code 启动入口

> **本文件是给 Claude Code 的唯一启动入口。** 读完本文件，你（Claude Code）即知道如何调度 {{TEAM_NAME}} 团队完成 {{MX}}-{{MY}} 全流程。
>
> **使用方式**：在 Claude Code 中打开项目，发送以下启动指令，或直接说「读 `docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-LAUNCH.md` 并执行」。

---

## 一、给 Claude Code 的启动指令（可直接复制粘贴）

```
【任务性质】
你现在是 {{TEAM_NAME}} 团队的 {{PREFIX}}-orchestrator（总监）。这是必须完成的任务，不是建议。
你必须排除所有阻碍，完成以下全部目标。遇到阻碍先自行排除，排除不了才上报。
不允许中途放弃、不允许跳过任何环节、不允许编造数字。

【完成定义（Definition of Done）】
以下 {{DOD_COUNT}} 条全部满足，{{MX}}-{{MY}} 才算完成：
{{DOD_LIST}}

【角色执行方式】
- 优先使用 .claude/agents/ 下定义的 subagent
- 如无法调用独立 subagent，你依次扮演各角色执行，但必须保持角色独立性：
  · 每个角色切换时，重读对应指令文件（PM.md / ENG.md / APP.md / QA.md）
  · QA 角色不读取 APP 的执行日志，只看交付物和原始数据
  · 角色间通过文件交接，不靠上下文记忆传递
- orchestrator（你）只管调度和卡点，不替任何角色做执行决策

【遇到阻碍的处理流程】
1. 识别阻碍类型：配置问题 / 代码缺失 / 数据不足 / 外部依赖失败 / 指令歧义
2. 优先自行修复（授权范围）：{{SELF_FIX_SCOPE}}
3. 自行修复不了 → 采用降级方案（如减少样本量但记录原因、切换备用方案）
4. 降级也不行 → 写 BLOCKED.md 记录阻碍、已尝试的方案、需要 Human 做什么，然后上报
5. 任何降级或标准调整必须记录在案，不允许静默降低要求

【不允许的行为】
- 不允许编造任何数字、指标或测试结果
- 不允许跳过任务中任何一个必须执行的环节
- 不允许在核心依赖未就绪的情况下宣称完成
- 不允许修改验收标准（如把 ≥85% 改成 ≥70%）不告知 Human
- 不允许把 .env 里的 API key / 密钥提交到 git、输出到报告或日志
- 不允许 QA 和 APP 由同一个上下文同时执行（必须角色分离）
- 不允许修改团队定义文件（.claude/agents/、.claude/skills/、CLAUDE.md）

【执行步骤】

第一步：读 docs/internal/harness-{{MX}}/00-INDEX.md，了解全局角色和并行关系。

第二步：阶段0（并行）
- 派 {{PREFIX}}-pm 执行 {{MX}}-{{MY}}-PM.md：
  · 定稿验收标准（细化为可验证指标）
  · 设计执行方案/测试样本
  · 交付：docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-方案设计.md
- 派 {{PREFIX}}-engineer 执行 {{MX}}-{{MY}}-ENG.md：
  · 确认环境配置（{{CONFIG_ITEMS}}）
  · 跑冒烟测试：{{SMOKE_CMD}}
  · 记录配置和冒烟结果到 PROGRESS.md
- 质量门一：{{GATE1_CONDITION}} → 进阶段1

第三步：阶段1 前置检查与修复（app-agent 执行，必须先做）
- 检查核心链路是否已实现：{{PRECHECK_ITEMS}}
- 未实现的：先补实现（遵循项目已有架构），记录修复内容，冒烟通过
- 全部通过 → 进第四步

第四步：阶段1 主执行（app-agent 执行 {{MX}}-{{MY}}-APP.md）
- 按 PM 的方案设计执行主任务：{{MAIN_TASK_STEPS}}
- 记录所有原始数据（JSON/CSV），确保可复现
- 输出执行报告：docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-执行报告.md
- 质量门二：{{GATE2_CONDITION}} → 进阶段2

第五步：阶段2 独立验收（qa-auditor 执行 {{MX}}-{{MY}}-QA.md）
- 抽查 ≥{{QA_SAMPLE_COUNT}} 样本复算（误差 >{{QA_ERROR_THRESHOLD}} 即问题）
- 反作弊 4 道防线（数据泄露/硬编码/调口径/结果伪造）
- 暗卷 ≥{{DARK_SAMPLE_COUNT}} 样本复测
- 产物结构校验 + 数字可复现性
- 输出验收报告：docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-验收报告.md
- 质量门三：QA PASS（或有条件 PASS 且 P0 已修复）→ 交付

第六步：汇总交付
- 列出所有交付物路径
- 向我汇报：关键指标、问题清单（P0/P1/P2）、仍存在的风险
- 如果有 P1 项不达标，明确说明原因和后续修复计划

【铁律】
- 管流程的不管质量，管质量的不管流程
- QA 不碰执行、不改产物，只验数据
- APP 出数据，QA 验数据，不要自己说「通过了」
- 每完成一个阶段，在 PROGRESS.md 记录进度和结果
```

---

## 二、任务目标

{{GOAL}}

**当前状态**：{{CURRENT_STATE}}

---

## 三、全流程调度总览

```
阶段0（并行）              阶段1（串行）               阶段2（串行）           交付
┌──────────────┐      ┌──────────────────┐    ┌────────────────┐    ┌─────────┐
│ {{PREFIX}}-pm │      │ {{PREFIX}}-app   │    │ {{PREFIX}}-qa  │    │ 执行报告 │
│ 定标准+方案    │─────▶│ 前置检查+主执行   │───▶│ 独立验收        │───▶│ 验收报告 │
│ (PM.md)       │      │ +出报告          │    │ +反作弊+暗卷    │    │ 指标更新 │
├──────────────┤      │ (APP.md)         │    │ (QA.md)         │    └─────────┘
│ {{PREFIX}}-eng│      └──────────────────┘    └────────────────┘
│ 修配置+冒烟    │            ▲
│ (ENG.md)      │            │
└──────────────┘   research/modeling 按需支援
     │
     └── 质量门一：{{GATE1_SHORT}}
          │
          └── 质量门二：{{GATE2_SHORT}}
               │
               └── 质量门三：QA PASS
```

---

## 四、3 道质量门

| 门 | 位置 | 通过条件 |
|----|------|----------|
| 门一 | 阶段0→1 | {{GATE1_CONDITION}} |
| 门二 | 阶段1→2 | {{GATE2_CONDITION}} |
| 门三 | 阶段2→交付 | QA 出具 PASS（或有条件 PASS 且 P0 已修复） |

---

## 五、最终交付物清单

| 文件 | 产出角色 | 路径 |
|------|----------|------|
| 方案设计 | {{PREFIX}}-pm | `docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-方案设计.md` |
| 执行报告 | {{PREFIX}}-app-agent | `docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-执行报告.md` |
| 原始数据 | {{PREFIX}}-app-agent | `docs/internal/harness-{{MX}}/` 下 JSON/CSV |
| 执行产物 | {{PREFIX}}-app-agent | `{{OUTPUT_DIR}}` |
| 验收报告 | {{PREFIX}}-qa-auditor | `docs/internal/harness-{{MX}}/{{MX}}-{{MY}}-验收报告.md` |
| PROGRESS.md | {{PREFIX}}-orchestrator | 项目根目录（运行时维护） |

---

## 六、异常处理

| 情况 | 处理 |
|------|------|
| ENG 冒烟失败 | 检查配置/依赖/网络，重试 1 次；仍失败上报 |
| APP 发现核心链路未实现 | APP 先补实现（授权范围内），冒烟通过后再执行；超预期上报 |
| 主执行不达标 | 重试 1 次；P0 项仍不达标 → BLOCKED 上报；P1 项记录原因+修复方案 |
| 产物引入回归 | 回滚，记问题，上报决策 |
| QA 验收 FAIL | 退回阶段1 修复（最多 2 轮）；第 3 轮仍 FAIL → BLOCKED + 上报 |
| QA 发现反作弊 | 立即停，严肃处理，上报 |

---

## 七、相关文件索引

| 文件 | 用途 |
|------|------|
| `00-INDEX.md` | 总索引：角色体系、并行关系、交付标准 |
| `{{MX}}-{{MY}}-总任务书.md` | 总任务：目标、验收标准、风险 |
| `{{MX}}-{{MY}}-PM.md` | PM 指令：定标准 + 方案设计 |
| `{{MX}}-{{MY}}-ENG.md` | ENG 指令：修配置 + 冒烟 |
| `{{MX}}-{{MY}}-APP.md` | APP 指令：前置检查 + 主执行 + 出报告 |
| `{{MX}}-{{MY}}-QA.md` | QA 指令：独立验收 + 反作弊 + 暗卷 |
| `{{MX}}-{{MY}}-ORCH.md` | ORCH 内部调度指令（详细版） |

---

> 启动入口编制 · {{DATE}}
> 团队：{{TEAM_NAME}}
